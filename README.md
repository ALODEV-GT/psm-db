# database

Supabase (Postgres) local dev stack for proyecto1 (Producciones Santa María).

Bootstrapped via SDD change `database-setup`. See tasks.md progress tracked in Engram (project `proyecto1`, topic_key `sdd/database-setup/tasks`).
